---
import Shell        from '../layouts/Shell.astro';
import DealsFeed    from '../components/deals/DealsFeed.astro';
import { getDeals } from '../lib/dataHub';

const deals = getDeals();
const dealYears = deals.map(d => new Date(d.date).getUTCFullYear()).filter(Number.isFinite);
const dealRange = dealYears.length ? `${Math.min(...dealYears)}–${Math.max(...dealYears)}` : 'No dates';
---

<Shell
  title="Deals & Investment"
  description="Major M&A, FDI, and investment transactions across Caribbean economies."
>
  <div class="page-wrapper deals-page">

    <header class="page-header fade-up fade-up-1">
      <span class="eyebrow">Deals &amp; Investment</span>
      <h1 class="page-title">Transactions</h1>
      <p class="page-sub">
        Headline M&amp;A, FDI, lending, bond, and concession deals across the Caribbean.
        All entries link to primary sources.
        Values are as reported; "undisclosed" where not publicly available.
      </p>
    </header>

    <div class="deals-meta-note mono fade-up">
      {deals.length} transactions · {dealRange} · Sources: company announcements, official releases, public filings, and credible regional business press
    </div>

    <section class="fade-up fade-up-2">
      <DealsFeed deals={deals} />
    </section>

  </div>
</Shell>

<style>
  .deals-page {
    padding-top: 36px;
    padding-bottom: 60px;
    display: flex;
    flex-direction: column;
    gap: var(--section-gap);
  }

  .page-title { font-size: var(--text-page); margin: 6px 0 10px; }
  .page-sub   { font-size: 15px; color: var(--ink-soft); max-width: 68ch; }

  .deals-meta-note {
    font-size: 11px;
    color: var(--ink-faint);
    letter-spacing: 0.04em;
    margin-top: calc(-1 * var(--section-gap) + var(--gap-md));
  }
</style>
